<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>
    <%= (typeof displayName !== 'undefined' && displayName) ? displayName : (userEmail || '') %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    html,
    body {
      height: 100%;
    }

    body {
      display: flex;
      flex-direction: column;
    }

    main {
      flex: 1;
    }
  </style>
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand fw-semibold" href="/">Project 1</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div id="mainNav" class="collapse navbar-collapse">
        <ul class="navbar-nav me-auto"></ul>
        <% if (isAuth) { %>
          <div class="d-flex align-items-center gap-2">
            <span class="text-light small">
              <%= userEmail %>
            </span>
            <a href="/createThread" class="btn btn-outline-light btn-sm">New Thread</a>
            <form action="/logout" method="post" class="m-0">
              <button type="submit" class="btn btn-primary btn-sm">Log out</button>
            </form>
          </div>
          <% } else { %>
            <div class="d-flex gap-2">
              <a class="btn btn-outline-light btn-sm" href="/loginForm">Log in</a>
              <a class="btn btn-primary btn-sm" href="/signupForm">Sign up</a>
            </div>
            <% } %>
      </div>
    </div>
  </nav>

  <main class="container py-4">
    <% if (flash) { %>
      <div class="alert alert-<%= flash.type || 'info' %>">
        <%= flash.message %>
      </div>
      <% } %>